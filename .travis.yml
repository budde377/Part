language: php
php:
  - 5.5
  - 5.6
before_script:
  - printf "\n" | pecl install imagick
  - composer install
  - mysql -e "CREATE DATABASE IF NOT EXISTS test_cms;" -uroot
  - mysql "test_cms" < db/CMSDatabase.sql
  - mysql -e "CREATE DATABASE IF NOT EXISTS test_postfix;" -uroot
  - mysql "test_cms" < db/SetupPostfixViews.sql

